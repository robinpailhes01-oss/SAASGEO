/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Active si on a besoin des Server Actions etendues plus tard
  },
};

export default nextConfig;
